import { PageWrapper, Title, TitleRow, Description } from '@/components/ui/Misc';

export default function ChatPage() {
  return (
    <PageWrapper>
      <TitleRow><Title>Live test</Title></TitleRow>
      <Description>Placeholder — implemented by the ChatMode workstream.</Description>
    </PageWrapper>
  );
}
